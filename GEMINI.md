# Mesh-SOS Project Instructions

Please strictly follow the development ground rules defined in [RULES.md](./RULES.md).

## Core Directives
- **Terminal Execution**: Always `cd` into `frontend` or `backend` before running build/dependency commands.
- **Git Operations**: Execute all Git commands from the root directory.
- **Conventions**: Adhere to the branch naming, commit message, and language-specific naming conventions specified in `RULES.md`.
